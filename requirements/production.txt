# Dependencies for this project when deployed to the Docker-based or cloud-based
# production environment.
# Note: This requirements file is also used when deploying this project to
#       cloud-based staging environments.

-r common.txt

django-auth-wall==0.2.0
psycopg2==2.7.4
pylibmc==1.5.2
