# PostgreSQL environment settings used by the local Docker environments.
# These environment variables are used by the official PostreSQL Docker images
# (https://hub.docker.com/_/postgres/) to provision the database.
#
# NOTE: This file is generated by the project Fabric script and it may contain
# passwords or other cryptographic secrets which SHOULD NOT be checked into a
# version control system.

POSTGRES_HOST=postgresql
POSTGRES_PORT=5432
POSTGRES_DB={{ postgres_db }}
POSTGRES_USER={{ postgres_user }}
POSTGRES_PASSWORD={{ postgres_password }}
